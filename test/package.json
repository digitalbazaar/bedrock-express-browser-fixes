{
  "name": "bedrock-web-vc-store-test",
  "version": "0.0.1-0",
  "private": true,
  "scripts": {
    "test": "npm run test-node",
    "test-node": "cross-env NODE_ENV=test mocha -r esm --preserve-symlinks -t 30000 -A -R ${REPORTER:-spec} --require test-mocha.js test.js 10-api.js",
    "coverage": "cross-env NODE_ENV=test nyc --reporter=lcov --reporter=text-summary npm run test-node",
    "coverage-ci": "cross-env NODE_ENV=test nyc --reporter=lcovonly npm run test-node",
    "coverage-report": "nyc report"
  },
  "dependencies": {
    "bedrock": "^4.1.1",
    "bedrock-express": "^3.2.0",
    "bedrock-express-browser-fixes": "file:..",
    "bedrock-mongodb": "^8.2.0",
    "bedrock-server": "^2.7.0",
    "bedrock-test": "^5.3.2",
    "cross-env": "^7.0.3",
    "nyc": "^15.1.0",
    "request": "^2.88.2",
    "supertest": "^6.1.3"
  },
  "nyc": {
    "excludeNodeModules": false,
    "include": [
      "node_modules/bedrock-express-browser-fixes/**"
    ],
    "exclude": [
      "node_modules/bedrock-express-browser-fixes/node_modules/**"
    ]
  }
}
